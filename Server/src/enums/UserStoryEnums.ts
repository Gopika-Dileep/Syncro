export enum UserStoryStatus {
  NEW = 'new',
  READY = 'ready',
  IN_SPRINT = 'In Sprint',
}

export enum UserStoryPriority {
  LOW = 'Low',
  MEDIUM = 'Medium',
  HIGH = 'High',
  CRITICAL = 'Critical',
}
